# Embedded Systems Analysis

## Building

Build the yarn docker image:

```sh
cd ./docker/yarn
./build.sh
cd ../..
```

Initialize the workspace:

```sh
./yarn.sh
```

Host The Site:

```sh
./yarn.sh start -h 0.0.0.0
```


